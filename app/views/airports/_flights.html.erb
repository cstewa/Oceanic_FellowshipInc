<%= form_tag 'selected_flight/' do %>
  <%= select_tag(:flight_id, options_from_collection_for_select(@flight_times, :id, :departure_time), :id => 'flight_times_pulldown') %>
  <%= submit_tag 'Submit Flight Time', :class => 'button' %>
<% end %>