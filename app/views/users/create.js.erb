var nav = $('nav.top-bar');
$('#signup_window').foundation('reveal', 'close');


// if there are errors:
<% if @user.valid? %>
nav.empty().append("<%= j render 'layouts/nav' %>");

// If there are NOT errors:
<% else %>


$('#errors').empty().append("<%= j render :partial => 'layouts/form_errors', :locals => {:errors => @user.errors.full_messages} %>");

<% end %>